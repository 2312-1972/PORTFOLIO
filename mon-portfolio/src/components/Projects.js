// src/components/Projects.js
import React from 'react';
import { animated } from 'react-spring';

const Projects = ({ style }) => {
  return (
    <animated.section id="projets" style={style}>
      {/* Vos projets */}
      <h2>Projets</h2>
      {/* Créez un composant de projet pour chaque réalisation */}
      {/* Utilisez une disposition à deux colonnes */}
    </animated.section>
  );
};

export default Projects;
